import 'dotenv/config';

console.log('[AIDEN Brain API v2] Starting...');

// Placeholder entry point - will be replaced with Express server in Phase 4
const PORT = process.env.PORT || 3000;
console.log(`[AIDEN Brain API v2] Ready on port ${PORT}`);
